<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    android:background="@drawable/bg_navbar"
    android:orientation="horizontal"
    android:gravity="center">

    <ImageView
        android:id="@+id/navbar_button_back"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:padding="10dp"
        android:src="@drawable/icon_back"
        android:background="@drawable/selector_navbar_button"
        android:contentDescription="@string/text_empty"
        android:visibility="invisible"
        android:clickable="true"/>

    <com.yunshu.widget.Label
        android:id="@+id/navbar_text_title"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_weight="1"
        android:singleLine="true"
        android:ellipsize="end"
        android:gravity="center"
        android:textColor="#ffffff"
        android:textSize="16sp"
        android:letterSpacing="0.1"/>

    <ImageView
        android:id="@+id/navbar_button_go"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:padding="10dp"
        android:src="@drawable/icon_confirm"
        android:background="@drawable/selector_navbar_button"
        android:contentDescription="@string/text_empty"
        android:visibility="invisible"
        android:clickable="true"/>

</LinearLayout>
